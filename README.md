    - 1️⃣ What is the difference between var, let, and const?
    Ans: Although these 3 keywords used for declaring variables but they also have some differences in there position. like, var has function scope but const, and let have block scope. We may change the value of var and let but cannot change the value of const.
    - 2️⃣ What is the spread operator (...)?
    Ans: In ES6, we found spread operator, which is very powerful for copying, merging and passing values within array and objects.
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans: forEach(): used to loop through an array, map(): used to transform each elements, filter():Used to select elements that match a condition.
    - 4️⃣ What is an arrow function?
    Ans: Its introduced in ES6. Basically, its a shorter version of writing an function using arrow syntax.
    - 5️⃣ What are template literals?
    Ans: Another powerful features in ES6. Just Using backticks, it gives us to write multiline strings and embedded expressions.
